// სწორი ავტორიზაციის მონაცემები
const correctEmail = "macharashvili.gio211@teachers.gov.ge";
const correctPassword = "Giorgi201124!";

// ავტორიზაციის ფორმის დამუშავება
document.getElementById("login-form").addEventListener("submit", function (e) {
    e.preventDefault(); // ფორმის გადატვირთვის თავიდან აცილება

    const email = document.getElementById("email").value;
    const password = document.getElementById("password").value;

    if (email === correctEmail && password === correctPassword) {
        document.getElementById("login-page").style.display = "none";
        document.getElementById("main-page").style.display = "block";
    } else {
        alert("არასწორი ელფოსტა ან პაროლი!");
    }
});

// თარიღის ავტომატური გამოჩენა
document.getElementById("current-date").textContent = new Date().toLocaleDateString();

// კლასების სიის დამუშავება
document.getElementById("class-form").addEventListener("submit", function (e) {
    e.preventDefault(); // ფორმის გადატვირთვის თავიდან აცილება

    const className = document.getElementById("class-input").value.trim();
    if (className) {
        const li = document.createElement("li");
        li.textContent = className;
        document.getElementById("class-list").appendChild(li);
        document.getElementById("class-input").value = ""; // ველის გასუფთავება
    }
});